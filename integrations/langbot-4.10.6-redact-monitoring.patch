diff --git a/source/langbot/libs/openclaw_weixin_api/client.py b/target/langbot/libs/openclaw_weixin_api/client.py
index d713f29..5d948e0 100644
--- a/langbot/libs/openclaw_weixin_api/client.py
+++ b/langbot/libs/openclaw_weixin_api/client.py
@@ -13,9 +13,12 @@ import base64
 import io
 import logging
 import os
+import ssl
 import struct
 import typing
 import uuid
+from dataclasses import dataclass
+from pathlib import Path
 from typing import Optional
 from urllib.parse import quote
 
@@ -60,6 +63,58 @@ DEFAULT_BOT_TYPE = '3'
 MAX_TEXT_CHUNK_SIZE = 2000
 
 
+class TLSConfigurationError(RuntimeError):
+    """Raised when an explicit OpenClaw CA override is not usable."""
+
+
+@dataclass(frozen=True)
+class TrustedCA:
+    bundle_path: str | None
+    ssl_context: ssl.SSLContext
+
+
+def _readable_bundle(path_value: str, source: str) -> str:
+    path = Path(path_value)
+    if not path.is_file() or not os.access(path, os.R_OK):
+        raise TLSConfigurationError(f'{source} must name a readable CA bundle')
+    try:
+        ssl.create_default_context(cafile=str(path))
+    except (OSError, ssl.SSLError) as exc:
+        raise TLSConfigurationError(f'{source} cannot be loaded as a CA bundle') from exc
+    return str(path)
+
+
+def configure_explicit_trusted_ca(configured_bundle: str | None = None) -> TrustedCA | None:
+    """Build client-local TLS trust only for an explicit CA override."""
+    candidates = (
+        ('adapter tls_ca_bundle', configured_bundle),
+        ('TLS_CA_BUNDLE', os.environ.get('TLS_CA_BUNDLE')),
+    )
+    for source, candidate in candidates:
+        if candidate:
+            bundle_path = _readable_bundle(candidate, source)
+            context = ssl.create_default_context(cafile=bundle_path)
+            return TrustedCA(bundle_path=bundle_path, ssl_context=context)
+    return None
+
+
+def is_certificate_verification_error(exc: BaseException) -> bool:
+    """Identify certificate failures without depending on provider error text."""
+    pending: list[BaseException] = [exc]
+    visited: set[int] = set()
+    while pending:
+        current = pending.pop()
+        if id(current) in visited:
+            continue
+        visited.add(id(current))
+        if isinstance(current, (aiohttp.ClientConnectorCertificateError, ssl.SSLCertVerificationError)):
+            return True
+        for nested in (getattr(current, '__cause__', None), getattr(current, '__context__', None), getattr(current, 'os_error', None)):
+            if isinstance(nested, BaseException):
+                pending.append(nested)
+    return False
+
+
 def _random_wechat_uin() -> str:
     """Generate the X-WECHAT-UIN header: random uint32 -> decimal string -> base64."""
     rand_bytes = os.urandom(4)
@@ -86,14 +141,18 @@ def _chunk_text(text: str, max_size: int = MAX_TEXT_CHUNK_SIZE) -> list[str]:
 class OpenClawWeixinClient:
     """Async client for the OpenClaw WeChat HTTP JSON API."""
 
-    def __init__(self, base_url: str, token: str):
+    def __init__(self, base_url: str, token: str, ssl_context: ssl.SSLContext | None = None):
         self.base_url = base_url.rstrip('/')
         self.token = token
+        self._ssl_context = ssl_context
         self._session: Optional[aiohttp.ClientSession] = None
 
     async def _get_session(self) -> aiohttp.ClientSession:
         if self._session is None or self._session.closed:
-            self._session = aiohttp.ClientSession()
+            if self._ssl_context is None:
+                self._session = aiohttp.ClientSession()
+            else:
+                self._session = aiohttp.ClientSession(connector=aiohttp.TCPConnector(ssl=self._ssl_context))
         return self._session
 
     async def close(self):
@@ -178,12 +237,10 @@ class OpenClawWeixinClient:
                 data = await resp.json(content_type=None)
 
         except (asyncio.TimeoutError, aiohttp.ServerTimeoutError):
-            return GetUpdatesResponse(ret=0, msgs=[], get_updates_buf=get_updates_buf)
+            raise
         except ApiError:
             raise
-        except Exception as e:
-            if 'timeout' in str(e).lower():
-                return GetUpdatesResponse(ret=0, msgs=[], get_updates_buf=get_updates_buf)
+        except Exception:
             raise
 
         return _parse_get_updates_response(data)
@@ -354,14 +411,7 @@ class OpenClawWeixinClient:
         # upload_param is an opaque token from the server — pass it as-is
         session = await self._get_session()
         cdn_url = f'{CDN_BASE_URL}/upload?encrypted_query_param={quote(upload_resp.upload_param, safe="")}&filekey={quote(filekey, safe="")}'
-        logger.debug(
-            'CDN upload: url=%s raw_size=%d encrypted_size=%d md5=%s aeskey=%s',
-            cdn_url,
-            len(file_bytes),
-            len(encrypted),
-            raw_md5,
-            encoded_key,
-        )
+        logger.debug('OpenClaw CDN upload started raw_size=%d encrypted_size=%d', len(file_bytes), len(encrypted))
 
         async with session.post(
             cdn_url,
@@ -371,7 +421,7 @@ class OpenClawWeixinClient:
         ) as resp:
             if resp.status != 200:
                 text = await resp.text()
-                logger.error('CDN upload failed: status=%d url=%s body=%s', resp.status, cdn_url, text[:500])
+                logger.error('OpenClaw CDN upload failed status=%d', resp.status)
                 raise ApiError(f'CDN upload failed: {resp.status} {text}', status=resp.status)
             download_param = resp.headers.get('x-encrypted-param', '')
 
@@ -474,7 +524,7 @@ class OpenClawWeixinClient:
             payload['aeskey'] = aeskey
 
         data = await self._post('ilink/bot/getuploadurl', payload)
-        logger.debug('get_upload_url response: %s', data)
+        logger.debug('OpenClaw upload URL acquired')
         return GetUploadUrlResponse(
             upload_param=data.get('upload_param'),
             thumb_upload_param=data.get('thumb_upload_param'),
@@ -498,9 +548,7 @@ class OpenClawWeixinClient:
                 )
             data = await resp.json(content_type=None)
 
-        logger.debug(
-            'fetch_qrcode response: qrcode=%s, img=%s', data.get('qrcode'), bool(data.get('qrcode_img_content'))
-        )
+        logger.debug('OpenClaw QR code response received')
 
         return QRCodeResponse(
             qrcode=data.get('qrcode'),
@@ -542,7 +590,7 @@ class OpenClawWeixinClient:
                         status=resp.status,
                     )
                 data = await resp.json(content_type=None)
-                logger.debug('QR status poll response: %s', data)
+                logger.debug('OpenClaw QR status response received')
         except (asyncio.TimeoutError, aiohttp.ServerTimeoutError):
             return QRStatusResponse(status='wait')
 
@@ -591,8 +639,8 @@ class OpenClawWeixinClient:
                     result = on_qrcode(last_qr_base64, qr_resp.qrcode_img_content)
                     if asyncio.iscoroutine(result) or asyncio.isfuture(result):
                         await result
-                except Exception as e:
-                    logger.warning('on_qrcode callback error: %s', e)
+                except Exception as exc:
+                    logger.warning('OpenClaw QR callback failed exception_class=%s', type(exc).__name__)
 
             # Poll until confirmed / expired / timeout
             loop = asyncio.get_running_loop()
@@ -601,8 +649,8 @@ class OpenClawWeixinClient:
             while loop.time() < deadline:
                 try:
                     status_resp = await self.poll_qrcode_status(qr_resp.qrcode)
-                except Exception as e:
-                    logger.error('Error polling QR status: %s', e)
+                except Exception as exc:
+                    logger.error('OpenClaw QR status poll failed exception_class=%s', type(exc).__name__)
                     await asyncio.sleep(2)
                     continue
 
@@ -611,8 +659,8 @@ class OpenClawWeixinClient:
                         cb_result = on_status(status_resp.status or 'unknown')
                         if asyncio.iscoroutine(cb_result) or asyncio.isfuture(cb_result):
                             await cb_result
-                    except Exception as e:
-                        logger.warning('on_status callback error: %s', e)
+                    except Exception as exc:
+                        logger.warning('OpenClaw QR status callback failed exception_class=%s', type(exc).__name__)
 
                 if status_resp.status == 'confirmed' and status_resp.bot_token:
                     new_base_url = status_resp.baseurl or self.base_url
diff --git a/source/langbot/pkg/api/http/controller/groups/platform/adapters.py b/target/langbot/pkg/api/http/controller/groups/platform/adapters.py
index 0e32f9d..05a1050 100644
--- a/langbot/pkg/api/http/controller/groups/platform/adapters.py
+++ b/langbot/pkg/api/http/controller/groups/platform/adapters.py
@@ -35,6 +35,10 @@ class AdaptersRouterGroup(group.RouterGroup):
         async def _() -> str:
             return self.success(data={'adapters': self.ap.platform_mgr.get_available_adapters_info()})
 
+        @self.route('/readiness', methods=['GET'])
+        async def _() -> str:
+            return self.success(data={'adapters': self.ap.platform_mgr.get_adapter_readiness()})
+
         @self.route('/<adapter_name>', methods=['GET'])
         async def _(adapter_name: str) -> str:
             adapter_info = self.ap.platform_mgr.get_available_adapter_info_by_name(adapter_name)
diff --git a/source/langbot/pkg/pipeline/monitoring_helper.py b/target/langbot/pkg/pipeline/monitoring_helper.py
index cc8ef6e..a3a9654 100644
--- a/langbot/pkg/pipeline/monitoring_helper.py
+++ b/langbot/pkg/pipeline/monitoring_helper.py
@@ -31,9 +31,8 @@ class MonitoringHelper:
     ) -> str:
         """Record the start of query processing, returns message_id"""
         try:
-            # Private content is persisted only by notebook-agent's tenant store.
-            # Do not duplicate external session identities here.
-            session_id = 'redacted-private-session'
+            # Check if session exists, if not, record session start
+            session_id = f'{query.launcher_type.value if hasattr(query.launcher_type, "value") else query.launcher_type}_{query.launcher_id}'
 
             # Get sender name from message event
             sender_name = None
@@ -44,8 +43,12 @@ class MonitoringHelper:
                     elif hasattr(query.message_event.sender, 'member_name'):
                         sender_name = query.message_event.sender.member_name
 
-            # LangBot monitoring must not duplicate private message content.
-            message_content = '[redacted by notebook-agent deployment]'
+            # Try to record message
+            # Use JSON serialization to preserve message chain structure (including image URLs, etc.)
+            if hasattr(query, 'message_chain') and hasattr(query.message_chain, 'model_dump'):
+                message_content = json.dumps(query.message_chain.model_dump(), ensure_ascii=False)
+            else:
+                message_content = str(query)
 
             # Variables will be updated in record_query_success after preproc stage sets them
             # Here we just record None, the full variables will be set when query completes
@@ -62,8 +65,8 @@ class MonitoringHelper:
                 platform=query.launcher_type.value
                 if hasattr(query.launcher_type, 'value')
                 else str(query.launcher_type),
-                user_id='redacted',
-                user_name=None,
+                user_id=query.sender_id,
+                user_name=sender_name,
                 runner_name=runner_name,
                 variables=None,  # Will be updated in record_query_success
             )
@@ -86,8 +89,8 @@ class MonitoringHelper:
                     platform=query.launcher_type.value
                     if hasattr(query.launcher_type, 'value')
                     else str(query.launcher_type),
-                    user_id='redacted',
-                    user_name=None,
+                    user_id=query.sender_id,
+                    user_name=sender_name,
                 )
 
             return message_id
diff --git a/source/langbot/pkg/pipeline/pipelinemgr.py b/target/langbot/pkg/pipeline/pipelinemgr.py
index ef189be..eb74573 100644
--- a/langbot/pkg/pipeline/pipelinemgr.py
+++ b/langbot/pkg/pipeline/pipelinemgr.py
@@ -281,6 +281,24 @@ class RuntimePipeline:
 
             i += 1
 
+    async def _reply_fail_closed(self, query: pipeline_query.Query, failure: Exception) -> None:
+        """Reply without exposing event data when a required bridge plugin is unavailable."""
+        try:
+            await query.adapter.reply_message(
+                message_source=query.message_event,
+                message=platform_message.MessageChain(
+                    [platform_message.Plain(text='知识库渠道暂时不可用，请稍后重试。')]
+                ),
+                quote_origin=False,
+            )
+        except Exception as reply_error:
+            self.ap.logger.warning(
+                f'Fail-closed reply failed for query {query.query_id}: {type(reply_error).__name__}'
+            )
+        self.ap.logger.warning(
+            f'Fail-closed required plugin event for query {query.query_id}: {type(failure).__name__}'
+        )
+
     async def process_query(self, query: pipeline_query.Query):
         """处理请求"""
         # Get monitoring metadata
@@ -317,6 +335,7 @@ class RuntimePipeline:
         try:
             # Get bound plugins for this pipeline
             bound_plugins = query.variables.get('_pipeline_bound_plugins', None)
+            required_plugins = self.ap.plugin_connector.required_plugins_for_pipeline(bound_plugins)
 
             # ======== 触发 MessageReceived 事件 ========
             event_type = (
@@ -334,7 +353,14 @@ class RuntimePipeline:
                 message_chain=query.message_chain,
             )
 
-            event_ctx = await self.ap.plugin_connector.emit_event(event_obj, bound_plugins)
+            try:
+                event_ctx = await self.ap.plugin_connector.emit_event(event_obj, bound_plugins)
+                self.ap.plugin_connector.validate_required_plugin_event(event_ctx, bound_plugins)
+            except Exception as plugin_error:
+                if required_plugins:
+                    await self._reply_fail_closed(query, plugin_error)
+                    return
+                raise
 
             if event_ctx.is_prevented_default():
                 self.ap.logger.debug(
diff --git a/source/langbot/pkg/pipeline/plugin_diagnostics.py b/target/langbot/pkg/pipeline/plugin_diagnostics.py
index 3e1195c..f0ca3b5 100644
--- a/langbot/pkg/pipeline/plugin_diagnostics.py
+++ b/langbot/pkg/pipeline/plugin_diagnostics.py
@@ -246,7 +246,6 @@ def _build_delivery_failure_payload(
 ) -> dict[str, Any]:
     details: dict[str, Any] = {
         'message_component_types': [component.__class__.__name__ for component in message_chain],
-        'message_preview': str(message_chain)[:200],
     }
     if is_approximate:
         details['attribution_warning'] = (
diff --git a/source/langbot/pkg/pipeline/process/process.py b/target/langbot/pkg/pipeline/process/process.py
index 27632f2..d25d143 100644
--- a/langbot/pkg/pipeline/process/process.py
+++ b/langbot/pkg/pipeline/process/process.py
@@ -36,9 +36,7 @@ class Processor(stage.PipelineStage):
         """Process"""
         message_text = str(query.message_chain).strip()
 
-        self.ap.logger.info(
-            f'Processing request from {query.launcher_type.value}_{query.launcher_id} ({query.query_id}): {message_text}'
-        )
+        self.ap.logger.info(f'Processing request {query.query_id} (message content redacted)')
 
         async def generator():
             cmd_prefix = self.ap.instance_config.data['command']['prefix']
diff --git a/source/langbot/pkg/platform/botmgr.py b/target/langbot/pkg/platform/botmgr.py
index 6e99520..a9f91cb 100644
--- a/langbot/pkg/platform/botmgr.py
+++ b/langbot/pkg/platform/botmgr.py
@@ -145,20 +145,12 @@ class RuntimeBot:
     ) -> None:
         """Record a discarded message in the monitoring system."""
         try:
-            if hasattr(message_chain, 'model_dump'):
-                message_content = json.dumps(message_chain.model_dump(), ensure_ascii=False)
-            else:
-                message_content = str(message_chain)
-
+            # Monitoring is not a private conversation store. Routing metadata
+            # is enough to diagnose a discard without persisting user content
+            # or external identity values.
+            message_content = '[redacted by notebook-agent deployment]'
             sender_name = None
-            if hasattr(message_event, 'sender'):
-                if hasattr(message_event.sender, 'nickname'):
-                    sender_name = message_event.sender.nickname
-                elif hasattr(message_event.sender, 'member_name'):
-                    sender_name = message_event.sender.member_name
-
-            # Use the same session_id format as monitoring_helper.py
-            session_id = f'{launcher_type}_{launcher_id}'
+            session_id = 'redacted-private-session'
             platform = launcher_type.value if hasattr(launcher_type, 'value') else str(launcher_type)
 
             await self.ap.monitoring_service.record_message(
@@ -171,7 +163,7 @@ class RuntimeBot:
                 status='discarded',
                 level='info',
                 platform=platform,
-                user_id=str(sender_id),
+                user_id='redacted',
                 user_name=sender_name,
             )
 
@@ -190,7 +182,7 @@ class RuntimeBot:
                     pipeline_id=self.PIPELINE_DISCARD,
                     pipeline_name=self.PIPELINE_DISCARD_DISPLAY_NAME,
                     platform=platform,
-                    user_id=str(sender_id),
+                    user_id='redacted',
                     user_name=sender_name,
                 )
         except Exception as e:
@@ -201,14 +193,8 @@ class RuntimeBot:
             event: platform_events.FriendMessage,
             adapter: abstract_platform_adapter.AbstractMessagePlatformAdapter,
         ):
-            image_components = [
-                component for component in event.message_chain if isinstance(component, platform_message.Image)
-            ]
-
             await self.logger.info(
-                f'{event.message_chain}',
-                images=image_components,
-                message_session_id=f'person_{event.sender.id}',
+                'Private message received [redacted]',
             )
 
             # Push to webhooks and check if pipeline should be skipped
@@ -262,14 +248,8 @@ class RuntimeBot:
             event: platform_events.GroupMessage,
             adapter: abstract_platform_adapter.AbstractMessagePlatformAdapter,
         ):
-            image_components = [
-                component for component in event.message_chain if isinstance(component, platform_message.Image)
-            ]
-
             await self.logger.info(
-                f'{event.message_chain}',
-                images=image_components,
-                message_session_id=f'group_{event.group.id}',
+                'Group message received [redacted]',
             )
 
             # Push to webhooks and check if pipeline should be skipped
@@ -464,6 +444,15 @@ class PlatformManager:
     def get_running_adapters(self) -> list[abstract_platform_adapter.AbstractMessagePlatformAdapter]:
         return [bot.adapter for bot in self.bots if bot.enable]
 
+    def get_adapter_readiness(self) -> list[dict]:
+        """Expose safe per-adapter readiness without changing process /healthz."""
+        readiness: list[dict] = []
+        for adapter in self.get_running_adapters():
+            get_readiness = getattr(adapter, 'get_readiness', None)
+            if callable(get_readiness):
+                readiness.append(get_readiness())
+        return readiness
+
     async def load_bots_from_db(self):
         self.ap.logger.info('Loading bots from db...')
 
diff --git a/source/langbot/pkg/platform/sources/openclaw_weixin.py b/target/langbot/pkg/platform/sources/openclaw_weixin.py
index 9253f90..dd42776 100644
--- a/langbot/pkg/platform/sources/openclaw_weixin.py
+++ b/langbot/pkg/platform/sources/openclaw_weixin.py
@@ -10,8 +10,8 @@ from __future__ import annotations
 
 import asyncio
 import base64
-import traceback
 import typing
+import time
 
 import pydantic
 import sqlalchemy
@@ -20,6 +20,10 @@ from langbot.libs.openclaw_weixin_api.client import (
     DEFAULT_BASE_URL,
     SESSION_EXPIRED_ERRCODE,
     OpenClawWeixinClient,
+    TLSConfigurationError,
+    TrustedCA,
+    configure_explicit_trusted_ca,
+    is_certificate_verification_error,
 )
 from langbot.libs.openclaw_weixin_api.types import (
     MessageItem,
@@ -248,6 +252,13 @@ class OpenClawWeixinAdapter(abstract_platform_adapter.AbstractMessagePlatformAda
     _polling: bool = pydantic.PrivateAttr(default=False)
     _poll_task: typing.Optional[asyncio.Task] = pydantic.PrivateAttr(default=None)
     _bot_uuid: typing.Optional[str] = pydantic.PrivateAttr(default=None)
+    _trusted_ca: typing.Optional[TrustedCA] = pydantic.PrivateAttr(default=None)
+    _readiness_state: str = pydantic.PrivateAttr(default='starting')
+    _last_error_code: typing.Optional[str] = pydantic.PrivateAttr(default=None)
+    _last_error_class: typing.Optional[str] = pydantic.PrivateAttr(default=None)
+    _last_success_monotonic: typing.Optional[float] = pydantic.PrivateAttr(default=None)
+    _retry_count: int = pydantic.PrivateAttr(default=0)
+    _next_retry_monotonic: typing.Optional[float] = pydantic.PrivateAttr(default=None)
 
     listeners: typing.Dict[
         typing.Type[platform_events.Event],
@@ -272,6 +283,71 @@ class OpenClawWeixinAdapter(abstract_platform_adapter.AbstractMessagePlatformAda
         """Called by BotManager to provide the bot's UUID for config persistence."""
         self._bot_uuid = bot_uuid
 
+    def get_readiness(self) -> dict[str, typing.Any]:
+        """Return safe operator state; never include tokens, identities, or payloads."""
+        now = time.monotonic()
+        return {
+            'adapter': self.name,
+            'state': self._readiness_state,
+            'error_code': self._last_error_code,
+            'exception_class': self._last_error_class,
+            'last_success_age_seconds': (
+                round(now - self._last_success_monotonic, 1) if self._last_success_monotonic is not None else None
+            ),
+            'retry_count': self._retry_count,
+            'next_retry_seconds': (
+                round(max(0.0, self._next_retry_monotonic - now), 1)
+                if self._next_retry_monotonic is not None
+                else None
+            ),
+            'ca_bundle_path': self._trusted_ca.bundle_path if self._trusted_ca else None,
+        }
+
+    def _mark_healthy(self) -> None:
+        self._readiness_state = 'healthy'
+        self._last_error_code = None
+        self._last_error_class = None
+        self._last_success_monotonic = time.monotonic()
+        self._retry_count = 0
+        self._next_retry_monotonic = None
+
+    def _mark_retrying(self, exc: BaseException, delay: float) -> None:
+        self._readiness_state = 'degraded'
+        self._last_error_code = 'upstream_unavailable'
+        self._last_error_class = type(exc).__name__
+        self._retry_count += 1
+        self._next_retry_monotonic = time.monotonic() + delay
+
+    def _mark_failed(self, error_code: str, exc: BaseException) -> None:
+        self._readiness_state = 'failed'
+        self._last_error_code = error_code
+        self._last_error_class = type(exc).__name__
+        self._next_retry_monotonic = None
+        self._polling = False
+
+    async def _replace_client(self, base_url: str, token: str) -> None:
+        previous_client = self.client
+        self.client = OpenClawWeixinClient(
+            base_url=base_url,
+            token=token,
+            ssl_context=self._trusted_ca.ssl_context if self._trusted_ca else None,
+        )
+        await previous_client.close()
+
+    async def _configure_explicit_tls(self) -> bool:
+        try:
+            self._trusted_ca = configure_explicit_trusted_ca(self.config.get('tls_ca_bundle'))
+            return True
+        except Exception as exc:
+            if isinstance(exc, TLSConfigurationError) or is_certificate_verification_error(exc):
+                self._mark_failed('certificate_verification_failed', exc)
+                await self.logger.error(
+                    'OpenClaw adapter state=failed error_code=certificate_verification_failed '
+                    f'exception_class={type(exc).__name__}; configure a readable verified CA bundle and restart.'
+                )
+                await self.client.close()
+                return False
+
     async def _persist_config(self) -> None:
         """Persist current self.config to the database so token survives restart."""
         if not self._bot_uuid:
@@ -283,18 +359,17 @@ class OpenClawWeixinAdapter(abstract_platform_adapter.AbstractMessagePlatformAda
                 .where(persistence_bot.Bot.uuid == self._bot_uuid)
                 .values(adapter_config=self.config)
             )
-        except Exception as e:
-            await self.logger.warning(f'Failed to persist adapter config: {e}')
+        except Exception as exc:
+            await self.logger.warning(
+                f'OpenClaw config persistence failed exception_class={type(exc).__name__}'
+            )
 
     async def _do_login(self) -> None:
         """Run the QR code login flow via client.login() and update config."""
         adapter_logger = self.logger
 
-        async def _on_qrcode(qr_base64: str, _qr_url: str):
-            await adapter_logger.info(
-                f'Please scan the QR code to login WeChat: {_qr_url}',
-                images=[platform_message.Image(base64=qr_base64)],
-            )
+        async def _on_qrcode(_qr_base64: str, _qr_url: str):
+            await adapter_logger.info('OpenClaw QR login requires secure operator handling.')
 
         login_result = await self.client.login(
             on_qrcode=_on_qrcode,
@@ -306,7 +381,7 @@ class OpenClawWeixinAdapter(abstract_platform_adapter.AbstractMessagePlatformAda
         if login_result.account_id:
             self.config['account_id'] = login_result.account_id
 
-        await self.logger.info(f'WeChat login successful! account_id={login_result.account_id}')
+        await self.logger.info('OpenClaw WeChat login successful.')
 
         # Persist token to database so it survives restart
         await self._persist_config()
@@ -345,9 +420,9 @@ class OpenClawWeixinAdapter(abstract_platform_adapter.AbstractMessagePlatformAda
                         if node.message_chain:
                             await self.send_message(target_type, target_id, node.message_chain)
 
-            except Exception:
+            except Exception as exc:
                 await self.logger.error(
-                    f'Failed to send component {type(component).__name__}: {traceback.format_exc()}'
+                    f'OpenClaw send failed exception_class={type(exc).__name__}'
                 )
 
     async def reply_message(
@@ -414,19 +489,28 @@ class OpenClawWeixinAdapter(abstract_platform_adapter.AbstractMessagePlatformAda
         base_url = self.config.get('base_url', DEFAULT_BASE_URL)
         token = self.config.get('token', '')
 
-        await self.logger.info('OpenClaw WeChat adapter starting...')
+        self._readiness_state = 'starting'
+        await self.logger.info('OpenClaw WeChat adapter state=starting')
+
+        if not await self._configure_explicit_tls():
+            return
 
         # QR code login flow when no token is provided
         if not token:
+            self._readiness_state = 'authenticating'
             await self.logger.info('No token configured, starting QR code login...')
             try:
                 await self._do_login()
-            except Exception as e:
-                await self.logger.error(f'QR code login failed: {e}')
+            except Exception as exc:
+                error_code = 'certificate_verification_failed' if is_certificate_verification_error(exc) else 'authentication_failed'
+                self._mark_failed(error_code, exc)
+                await self.logger.error(
+                    f'OpenClaw adapter state=failed error_code={error_code} exception_class={type(exc).__name__}'
+                )
                 raise
 
         # Rebuild client with the (possibly updated) config
-        self.client = OpenClawWeixinClient(
+        await self._replace_client(
             base_url=self.config.get('base_url', base_url),
             token=self.config.get('token', token),
         )
@@ -435,7 +519,7 @@ class OpenClawWeixinAdapter(abstract_platform_adapter.AbstractMessagePlatformAda
 
         # Start the long-poll loop
         self._poll_task = asyncio.create_task(self._poll_loop())
-        await self.logger.info('OpenClaw WeChat adapter running')
+        await self.logger.info('OpenClaw WeChat adapter state=starting; awaiting successful poll')
 
         try:
             await self._poll_task
@@ -474,9 +558,10 @@ class OpenClawWeixinAdapter(abstract_platform_adapter.AbstractMessagePlatformAda
                     if is_session_expired:
                         await self.logger.error('OpenClaw WeChat session expired, attempting re-login...')
                         try:
+                            self._readiness_state = 'authenticating'
                             await self._do_login()
                             # Rebuild client with new credentials
-                            self.client = OpenClawWeixinClient(
+                            await self._replace_client(
                                 base_url=self.config.get('base_url', DEFAULT_BASE_URL),
                                 token=self.config.get('token', ''),
                             )
@@ -484,18 +569,25 @@ class OpenClawWeixinAdapter(abstract_platform_adapter.AbstractMessagePlatformAda
                             get_updates_buf = ''
                             backoff_delay = 1.0
                             continue
-                        except Exception:
-                            await self.logger.error(f'Re-login failed: {traceback.format_exc()}')
+                        except Exception as exc:
+                            error_code = 'certificate_verification_failed' if is_certificate_verification_error(exc) else 'authentication_failed'
+                            self._mark_failed(error_code, exc)
+                            await self.logger.error(
+                                f'OpenClaw adapter state=failed error_code={error_code} exception_class={type(exc).__name__}'
+                            )
                             break
 
-                    await self.logger.error(
-                        f'OpenClaw getUpdates failed: ret={resp.ret} errcode={resp.errcode} errmsg={resp.errmsg}'
+                    self._mark_retrying(RuntimeError('upstream response error'), backoff_delay)
+                    await self.logger.warning(
+                        'OpenClaw adapter state=degraded error_code=upstream_unavailable '
+                        f'exception_class=OpenClawResponseError retry_count={self._retry_count}'
                     )
                     await asyncio.sleep(backoff_delay)
                     backoff_delay = min(backoff_delay * 2, max_backoff)
                     continue
 
                 backoff_delay = 1.0
+                self._mark_healthy()
 
                 if resp.get_updates_buf:
                     get_updates_buf = resp.get_updates_buf
@@ -503,13 +595,26 @@ class OpenClawWeixinAdapter(abstract_platform_adapter.AbstractMessagePlatformAda
                 for msg in resp.msgs:
                     try:
                         await self._handle_inbound_message(msg)
-                    except Exception:
-                        await self.logger.error(f'Error handling message: {traceback.format_exc()}')
+                    except Exception as exc:
+                        await self.logger.error(
+                            f'OpenClaw message handling failed exception_class={type(exc).__name__}'
+                        )
 
             except asyncio.CancelledError:
                 break
-            except Exception:
-                await self.logger.error(f'OpenClaw poll error: {traceback.format_exc()}')
+            except Exception as exc:
+                if is_certificate_verification_error(exc):
+                    self._mark_failed('certificate_verification_failed', exc)
+                    await self.logger.error(
+                        'OpenClaw adapter state=failed error_code=certificate_verification_failed '
+                        f'exception_class={type(exc).__name__}; configure a readable verified CA bundle and restart.'
+                    )
+                    break
+                self._mark_retrying(exc, backoff_delay)
+                await self.logger.warning(
+                    'OpenClaw adapter state=degraded error_code=upstream_unavailable '
+                    f'exception_class={type(exc).__name__} retry_count={self._retry_count}'
+                )
                 await asyncio.sleep(backoff_delay)
                 backoff_delay = min(backoff_delay * 2, max_backoff)
 
@@ -560,12 +665,14 @@ class OpenClawWeixinAdapter(abstract_platform_adapter.AbstractMessagePlatformAda
                 #         video_bytes = await self.client.download_media(item.video_item.media)
                 #         item.video_item._downloaded_bytes = video_bytes
 
-            except Exception:
-                await self.logger.warning(f'Failed to download CDN media: {traceback.format_exc()}')
+            except Exception as exc:
+                await self.logger.warning(f'OpenClaw media download failed exception_class={type(exc).__name__}')
 
     async def kill(self) -> bool:
         """Stop the adapter."""
         self._polling = False
+        if self._readiness_state != 'failed':
+            self._readiness_state = 'stopped'
         if self._poll_task and not self._poll_task.done():
             self._poll_task.cancel()
             try:
diff --git a/source/langbot/pkg/plugin/connector.py b/target/langbot/pkg/plugin/connector.py
index 6075d4b..a8b907b 100644
--- a/langbot/pkg/plugin/connector.py
+++ b/langbot/pkg/plugin/connector.py
@@ -40,6 +40,18 @@ class PluginRuntimeNotConnectedError(RuntimeError):
     """Raised when plugin runtime operations are requested before connection."""
 
 
+class RequiredPluginConfigurationError(ValueError):
+    """Raised when required plugin configuration is malformed."""
+
+
+class RequiredPluginNotReadyError(RuntimeError):
+    """Raised when a required plugin is unavailable before an adapter may receive messages."""
+
+
+class RequiredPluginEventError(RuntimeError):
+    """Raised when a required plugin did not safely handle a pipeline event."""
+
+
 class PluginRuntimeConnector(ManagedRuntimeConnector):
     """Plugin runtime connector"""
 
@@ -70,6 +82,108 @@ class PluginRuntimeConnector(ManagedRuntimeConnector):
         super().__init__(ap)
         self.runtime_disconnect_callback = runtime_disconnect_callback
         self.is_enable_plugin = self.ap.instance_config.data.get('plugin', {}).get('enable', True)
+        plugin_config = self.ap.instance_config.data.get('plugin', {})
+        self.required_plugins = self._parse_required_plugins(plugin_config.get('required_plugins', []))
+        self.required_plugins_ready_timeout_seconds = self._parse_ready_timeout(
+            plugin_config.get('required_plugins_ready_timeout_seconds', 30)
+        )
+        self._runtime_connected = asyncio.Event()
+        self._required_plugins_ready = asyncio.Event()
+
+    @staticmethod
+    def _parse_required_plugins(value: Any) -> tuple[str, ...]:
+        if value is None:
+            return ()
+        if isinstance(value, str):
+            value = [item.strip() for item in value.split(',') if item.strip()]
+        if not isinstance(value, list):
+            raise RequiredPluginConfigurationError('plugin.required_plugins must be a list of author/name values')
+
+        required_plugins: list[str] = []
+        for item in value:
+            if not isinstance(item, str):
+                raise RequiredPluginConfigurationError('plugin.required_plugins entries must be strings')
+            plugin_ref = item.strip()
+            author, separator, plugin_name = plugin_ref.partition('/')
+            if not separator or not author or not plugin_name or '/' in plugin_name:
+                raise RequiredPluginConfigurationError(
+                    'plugin.required_plugins entries must use the author/name format'
+                )
+            if plugin_ref not in required_plugins:
+                required_plugins.append(plugin_ref)
+        return tuple(required_plugins)
+
+    @staticmethod
+    def _parse_ready_timeout(value: Any) -> float:
+        try:
+            timeout = float(value)
+        except (TypeError, ValueError) as exc:
+            raise RequiredPluginConfigurationError(
+                'plugin.required_plugins_ready_timeout_seconds must be a positive number'
+            ) from exc
+        if timeout <= 0:
+            raise RequiredPluginConfigurationError(
+                'plugin.required_plugins_ready_timeout_seconds must be a positive number'
+            )
+        return timeout
+
+    @staticmethod
+    def _emitted_plugin_ref(plugin: Any) -> str | None:
+        """Extract author/name from PluginContainer.model_dump()."""
+        if not isinstance(plugin, dict):
+            return None
+        component_manifest = plugin.get('manifest')
+        if not isinstance(component_manifest, dict):
+            return None
+        manifest = component_manifest.get('manifest', component_manifest)
+        metadata = manifest.get('metadata') if isinstance(manifest, dict) else None
+        author = metadata.get('author') if isinstance(metadata, dict) else None
+        plugin_name = metadata.get('name') if isinstance(metadata, dict) else None
+        if not isinstance(author, str) or not isinstance(plugin_name, str):
+            return None
+        return f'{author}/{plugin_name}'
+
+    def _mark_runtime_disconnected(self) -> None:
+        self._runtime_connected.clear()
+        self._required_plugins_ready.clear()
+
+    def required_plugins_for_pipeline(self, bound_plugins: list[str] | None) -> tuple[str, ...]:
+        """Return configured required plugins explicitly bound to this pipeline.
+
+        A pipeline with ``enable_all_plugins`` does not opt into the strict guard:
+        it retains LangBot's upstream plugin semantics. Notebook Agent pipelines
+        explicitly bind their bridge plugin and are therefore fail closed.
+        """
+        if bound_plugins is None or not self.required_plugins:
+            return ()
+        bound_plugin_set = set(bound_plugins)
+        return tuple(plugin_ref for plugin_ref in self.required_plugins if plugin_ref in bound_plugin_set)
+
+    def validate_required_plugin_event(self, event_ctx: context.EventContext, bound_plugins: list[str] | None) -> None:
+        """Require configured bridge plugins to handle the current early event."""
+        required_plugins = self.required_plugins_for_pipeline(bound_plugins)
+        if not required_plugins:
+            return
+        if not self._runtime_connected.is_set():
+            raise RequiredPluginNotReadyError('plugin runtime is not connected')
+
+        emitted_plugins = getattr(event_ctx, '_emitted_plugins', [])
+        emitted_plugin_refs: set[str] = set()
+        if isinstance(emitted_plugins, list):
+            for plugin in emitted_plugins:
+                plugin_ref = self._emitted_plugin_ref(plugin)
+                if plugin_ref:
+                    emitted_plugin_refs.add(plugin_ref)
+
+        missing_plugins = [plugin_ref for plugin_ref in required_plugins if plugin_ref not in emitted_plugin_refs]
+        if missing_plugins:
+            raise RequiredPluginEventError('required plugin did not handle the event')
+        if not event_ctx.is_prevented_default():
+            raise RequiredPluginEventError('required plugin did not prevent the default pipeline')
+
+        # A successfully handled event is stronger evidence than an earlier
+        # status query and also marks a reconnected runtime ready again.
+        self._required_plugins_ready.set()
 
     async def heartbeat_loop(self):
         while True:
@@ -89,6 +203,7 @@ class PluginRuntimeConnector(ManagedRuntimeConnector):
             async def disconnect_callback(
                 rchandler: handler.RuntimeConnectionHandler,
             ) -> bool:
+                self._mark_runtime_disconnected()
                 if platform.get_platform() == 'docker' or platform.use_websocket_to_connect_plugin_runtime():
                     self.ap.logger.error('Disconnected from plugin runtime, trying to reconnect...')
                     await self.runtime_disconnect_callback(self)
@@ -103,6 +218,7 @@ class PluginRuntimeConnector(ManagedRuntimeConnector):
 
             self.handler_task = asyncio.create_task(self.handler.run())
             _ = await self.handler.ping()
+            self._runtime_connected.set()
             # Push the configured marketplace (Space) URL to the runtime so it
             # downloads plugins from the same Space LangBot is bound to, rather
             # than relying on the runtime's own env/default.
@@ -114,7 +230,10 @@ class PluginRuntimeConnector(ManagedRuntimeConnector):
                 except Exception as e:
                     self.ap.logger.warning(f'Failed to push runtime config: {e}')
             self.ap.logger.info('Connected to plugin runtime.')
-            await self.handler_task
+            try:
+                await self.handler_task
+            finally:
+                self._mark_runtime_disconnected()
 
         task: asyncio.Task | None = None
 
@@ -128,6 +247,7 @@ class PluginRuntimeConnector(ManagedRuntimeConnector):
                 ctrl: ws_client_controller.WebSocketClientController,
                 exc: Exception = None,
             ) -> None:
+                self._mark_runtime_disconnected()
                 if exc is not None:
                     self.ap.logger.error(f'Failed to connect to plugin runtime({ws_url}): {exc}')
                 else:
@@ -153,6 +273,7 @@ class PluginRuntimeConnector(ManagedRuntimeConnector):
                 ctrl: ws_client_controller.WebSocketClientController,
                 exc: Exception = None,
             ) -> None:
+                self._mark_runtime_disconnected()
                 if exc is not None:
                     self.ap.logger.error(f'(windows) Failed to connect to plugin runtime({ws_url}): {exc}')
                 else:
@@ -185,10 +306,54 @@ class PluginRuntimeConnector(ManagedRuntimeConnector):
         asyncio.create_task(task)
 
     async def initialize_plugins(self):
-        pass
+        """Wait for configured required plugins before adapters start receiving messages.
+
+        The plugin runtime starts asynchronously. A fixed delay merely moves the
+        race, so this waits for both a successful runtime connection and the
+        runtime's authoritative ``initialized`` status until a configured
+        deadline is reached.
+        """
+        if not self.required_plugins:
+            return
+        if not self.is_enable_plugin:
+            raise RequiredPluginNotReadyError('required plugins configured while the plugin system is disabled')
+
+        loop = asyncio.get_running_loop()
+        deadline = loop.time() + self.required_plugins_ready_timeout_seconds
+        retry_delay = 0.05
+
+        while True:
+            remaining = deadline - loop.time()
+            if remaining <= 0:
+                raise RequiredPluginNotReadyError('timed out waiting for required plugins to initialize')
+
+            try:
+                await asyncio.wait_for(self._runtime_connected.wait(), timeout=remaining)
+                statuses = await asyncio.gather(
+                    *(self.get_plugin_info(*plugin_ref.split('/', 1)) for plugin_ref in self.required_plugins)
+                )
+            except asyncio.TimeoutError as exc:
+                raise RequiredPluginNotReadyError('timed out waiting for required plugins to initialize') from exc
+            except Exception as exc:
+                self.ap.logger.debug(f'Required plugin readiness check failed: {type(exc).__name__}')
+                statuses = []
+
+            if len(statuses) == len(self.required_plugins) and all(
+                isinstance(status, dict) and status.get('status') == 'initialized' for status in statuses
+            ):
+                self._required_plugins_ready.set()
+                self.ap.logger.info('Required plugins initialized; message adapters may start.')
+                return
+
+            self._required_plugins_ready.clear()
+            remaining = deadline - loop.time()
+            if remaining <= 0:
+                raise RequiredPluginNotReadyError('timed out waiting for required plugins to initialize')
+            await asyncio.sleep(min(retry_delay, remaining))
+            retry_delay = min(retry_delay * 2, 0.5)
 
     async def ping_plugin_runtime(self):
-        if not hasattr(self, 'handler'):
+        if not self._runtime_connected.is_set() or not hasattr(self, 'handler'):
             raise PluginRuntimeNotConnectedError('Plugin runtime is not connected')
 
         return await self.handler.ping()
diff --git a/source/langbot/templates/config.yaml b/target/langbot/templates/config.yaml
index f4ae79b..3599f8b 100644
--- a/langbot/templates/config.yaml
+++ b/langbot/templates/config.yaml
@@ -117,6 +117,10 @@ storage:
 plugin:
     enable: true
     runtime_ws_url: 'ws://langbot_plugin_runtime:5400/control/ws'
+    # Required plugins must be initialized before enabled message adapters start.
+    # Empty keeps the upstream 4.10.6 startup behavior unchanged.
+    required_plugins: []
+    required_plugins_ready_timeout_seconds: 30
     enable_marketplace: true
     display_plugin_debug_url: 'ws://localhost:5401/plugin/debug/ws'
     binary_storage:

diff --git a/source/langbot/pkg/api/http/controller/groups/platform/bots.py b/target/langbot/pkg/api/http/controller/groups/platform/bots.py
--- a/langbot/pkg/api/http/controller/groups/platform/bots.py
+++ b/langbot/pkg/api/http/controller/groups/platform/bots.py
@@ -60,5 +60,4 @@
-            except Exception as e:
-                import traceback
-
-                traceback.print_exc()
-                return self.http_status(500, -1, f'Failed to send message: {str(e)}')
+            except Exception:
+                # Outbound message failures may contain target/content or
+                # credentials. Keep the public response fixed and redacted.
+                return self.http_status(500, -1, 'Failed to send message')

diff --git a/source/langbot/pkg/api/http/controller/main.py b/target/langbot/pkg/api/http/controller/main.py
--- a/langbot/pkg/api/http/controller/main.py
+++ b/langbot/pkg/api/http/controller/main.py
@@ -78,7 +78,7 @@ class HTTPController:
             self.ap.task_mgr.create_task(
                 exception_handler(
-                    host='0.0.0.0',
+                    host='127.0.0.1',
                     port=self.ap.instance_config.data['api']['port'],
                     shutdown_trigger=shutdown_trigger_placeholder,
                 ),
